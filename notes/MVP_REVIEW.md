## Settings updates
- the title of the settings window is "Infrastructure Office". Rename to Settings
- breadcrumb bar at the top with the "Back" button is not vertically alighned to center all buttons. please vertically center them
### Ollama settings
- this section should contain the default ollama settings. onsection for local and another optional section for remote, which can be overriden on a per agent basis.
- the ollama model settings should include management settings:
   - when selecting a model for "Lead Model" (for example) this should be a drop down of available models on the running ollama service.
   - user should be able to manually enter a model name and if valid, ollama service will pull it and run it
   - user shoudl be able to delete and existing model.

